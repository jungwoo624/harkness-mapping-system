/** 로그인 / 가입 (placeholder) */
export function LoginPage() {
  return (
    <main className="mx-auto max-w-md px-4 py-16">
      <h1 className="text-3xl font-bold text-slate-900">로그인 / 가입</h1>
      <p className="mt-3 text-slate-500">로그인·회원가입 폼이 추가될 예정입니다.</p>
    </main>
  )
}
